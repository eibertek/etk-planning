
import React from 'react'
import { View, Text } from 'react-native'

interface Props {};

const TaskModal: React.FunctionComponent<Props> = (props:Props) => {
    return (
        <View>
            <Text>New Task</Text>
        </View>
    )
}

export default TaskModal;
